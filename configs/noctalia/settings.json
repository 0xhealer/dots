{
  "_comment": "Confirmed v4 (Quickshell/Qt) from the running install (noctalia-shell-4.7.7). This JSON shape is correct for that version -- v5 (beta, no Qt) would need settings.toml instead, doesn't apply here.",
  "theme": {
    "source": "wallpaper",
    "predefinedScheme": "Catppuccin Mocha",
    "darkMode": true
  },
  "wallpaper": {
    "enabled": true,
    "directory": "~/Pictures/Wallpapers",
    "transitionOnStartup": true
  },
  "templates": {
    "_comment": "THIS was the actual bug last time -- Noctalia defaults activeTemplates to an empty list, so wallpaper-driven colors were generated but never written into any app's config until you list the app here. Confirmed real template ids: kitty, niri, gtk3, gtk4, qt. For built-in templates like these, Noctalia writes the color file AND edits the app's own config to include it automatically -- no manual kitty.conf edit needed (that's only required for user-defined templates, which this isn't).",
    "activeTemplates": [
      { "id": "kitty", "active": true },
      { "id": "niri", "active": true },
      { "id": "gtk3", "active": true },
      { "id": "gtk4", "active": true },
      { "id": "qt", "active": true }
    ],
    "enableUserTheming": true
  },
  "bar": {
    "_comment": "backgroundOpacity/capsuleOpacity default to 1 (fully opaque) per Noctalia's own settings-default.json -- this was the actual missing piece for blur/transparency, not the compositor-side layer_rule which was already correct. Field names and the 0.5 example value confirmed from a real user's settings.json pasted in noctalia-shell issue #1864.",
    "position": "top",
    "backgroundOpacity": 0.85,
    "capsuleOpacity": 0.85
  },
  "dock": {
    "backgroundOpacity": 0.85
  },
  "notifications": {
    "backgroundOpacity": 0.9
  },
  "osd": {
    "backgroundOpacity": 0.9
  },
  "ui": {
    "panelBackgroundOpacity": 0.85
  }
}
